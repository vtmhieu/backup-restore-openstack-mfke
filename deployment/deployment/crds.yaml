---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: pvcs.snapshot.mfke.io
spec:
  group: snapshot.mfke.io
  names:
    kind: Pvc
    listKind: PvcList
    plural: pvcs
    singular: pvc
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: Pvc is the Schema for the pvcs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PvcSpec defines the desired state of Pvc
            properties:
              namespaceShoot:
                description: namespace of pvc
                type: string
            type: object
          status:
            description: PvcStatus defines the observed state of Pvc
            properties:
              conditions:
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              pvcList:
                description: total names of pvc in namespace
                items:
                  description: PvcDetail defines the sum-up information of all PVC
                    in shoot cluster
                  properties:
                    accessMode:
                      items:
                        type: string
                      type: array
                    creationTime:
                      type: string
                    namespace:
                      type: string
                    pvcName:
                      type: string
                    resource:
                      type: string
                    sourceSnapshotName:
                      type: string
                    status:
                      type: string
                    storageClass:
                      type: string
                    volumeMode:
                      type: string
                    volumeName:
                      type: string
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: pvsnapshots.snapshot.mfke.io
spec:
  group: snapshot.mfke.io
  names:
    kind: PvSnapshot
    listKind: PvSnapshotList
    plural: pvsnapshots
    singular: pvsnapshot
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: CreationTimestamp is a timestamp representing the server time when
        this object was created
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: PvSnapshot is the Schema for the pvsnapshots API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PvSnapshotSpec defines the desired state of PvSnapshot
            properties:
              namespace:
                type: string
              pvcName:
                type: string
            type: object
          status:
            description: PvSnapshotStatus defines the observed state of PvSnapshot
            properties:
              conditions:
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              items:
                items:
                  properties:
                    creationTime:
                      type: string
                    namespace:
                      type: string
                    readyToUse:
                      type: boolean
                    restoreSize:
                      type: string
                    snapshotContentName:
                      type: string
                    snapshotName:
                      type: string
                    sourcePvcName:
                      type: string
                    volumeSnapshotClassName:
                      type: string
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: restorepvcs.snapshot.mfke.io
spec:
  group: snapshot.mfke.io
  names:
    kind: RestorePvc
    listKind: RestorePvcList
    plural: restorepvcs
    singular: restorepvc
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The age of the Scheduler
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - jsonPath: .status.sourceSnapshotName
      name: sourceSnapshotName
      type: string
    - jsonPath: .status.sourceNamespace
      name: sourceNamespace
      type: string
    - jsonPath: .status.desNamespace
      name: destinateNamespace
      type: string
    - jsonPath: .status.accessMode
      name: accessMode
      type: string
    - jsonPath: .status.status
      name: status
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: RestorePvc is the Schema for the restorepvcs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RestorePvcSpec defines the desired state of RestorePvc
            properties:
              accessModes:
                items:
                  type: string
                type: array
              conflictHandling:
                type: string
              desNamespace:
                type: string
              snapshotName:
                description: |-
                  Define fields for RestorePVC spec
                  RestorePvcName   string                              `json:"restorePvcName,omitempty"`
                type: string
              sourceNamespace:
                type: string
              storage:
                type: string
            type: object
          status:
            description: RestorePvcStatus defines the observed state of RestorePvc
            properties:
              accessMode:
                items:
                  type: string
                type: array
              conditions:
                description: Define fields for RestorePVC status
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              conflictHandling:
                type: string
              creationStatus:
                type: string
              creationTime:
                format: date-time
                type: string
              desNamespace:
                type: string
              resource:
                type: string
              restorePvcName:
                type: string
              sourceNamespace:
                type: string
              sourceSnapshotName:
                type: string
              status:
                type: string
              storageClass:
                type: string
              volumeMode:
                type: string
              volumeName:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: schedulersnapshots.snapshot.mfke.io
spec:
  group: snapshot.mfke.io
  names:
    kind: SchedulerSnapshot
    listKind: SchedulerSnapshotList
    plural: schedulersnapshots
    singular: schedulersnapshot
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The age of the Scheduler
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - jsonPath: .status.snapshotScheduler.pvcName
      name: PvcName
      type: string
    - description: The number of checks that passed
      jsonPath: .status.snapshotScheduler.namespace
      name: Namespace
      priority: 1
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: SchedulerSnapshot is the Schema for the schedulersnapshots API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SchedulerSnapshotSpec defines the desired state of SchedulerSnapshot
            properties:
              enabled:
                type: boolean
              snapshotScheduler:
                properties:
                  configSnapshotClass:
                    properties:
                      includeClusterResources:
                        description: |-
                          IncludeClusterResources, if true, will include cluster-wide resources in
                          the snapshot. Otherwise, only namespace-scoped resources will be included.
                        type: boolean
                      includeNamespaces:
                        description: |-
                          IncludeNamespaces is the list of namespaces to include in the snapshot.
                          If nil, all namespaces will be included. If empty, no namespaces will be
                          included.
                        items:
                          type: string
                        type: array
                    type: object
                  pvcSnapshotClass:
                    items:
                      properties:
                        namespace:
                          type: string
                        pvcName:
                          type: string
                      type: object
                    type: array
                  retentionPolicy:
                    description: SnapshotRetentionPolicy defines the policy for retaining
                      snapshots.
                    properties:
                      max:
                        type: integer
                      timeUnits:
                        type: string
                    type: object
                  schedule:
                    items:
                      properties:
                        location:
                          type: string
                        start:
                          type: string
                      type: object
                    type: array
                type: object
            type: object
          status:
            description: SchedulerSnapshotStatus defines the observed state of SchedulerSnapshot
            properties:
              conditions:
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: snapshots.snapshot.mfke.io
spec:
  group: snapshot.mfke.io
  names:
    kind: Snapshot
    listKind: SnapshotList
    plural: snapshots
    singular: snapshot
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The age of the Scheduler
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - jsonPath: .status.pvcName
      name: pvcName
      type: string
    - jsonPath: .status.namespace
      name: shootNamespace
      type: string
    - jsonPath: .status.creationStatus
      name: creationStatus
      type: string
    - jsonPath: .status.creationTime
      name: creationTime
      type: string
    - jsonPath: .status.readyToUse
      name: readyToUse
      type: boolean
    - jsonPath: .status.snapshotType
      name: snapshotType
      type: string
    - jsonPath: .status.restoreSize
      name: restoreSize
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Snapshot is the Schema for the snapshots API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SnapshotSpec defines the desired state of Snapshot
            properties:
              namespace:
                type: string
              numInUse:
                type: integer
              pvcName:
                type: string
              snapshotName:
                type: string
              snapshotType:
                type: string
            type: object
          status:
            description: CreateSnapshotStatus defines the observed state of CreateSnapshot
            properties:
              conditions:
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              creationStatus:
                type: string
              creationTime:
                type: string
              inUse:
                type: boolean
              namespace:
                type: string
              pvcName:
                type: string
              readyToUse:
                type: boolean
              restoreSize:
                type: string
              snapshotContentName:
                type: string
              snapshotName:
                type: string
              snapshotType:
                type: string
              volumeSnapshotClassName:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: syncsnapshots.snapshot.mfke.io
spec:
  group: snapshot.mfke.io
  names:
    kind: SyncSnapshot
    listKind: SyncSnapshotList
    plural: syncsnapshots
    singular: syncsnapshot
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: SyncSnapshot is the Schema for the syncsnapshots API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SyncSnapshotSpec defines the desired state of SyncSnapshot
            type: object
          status:
            description: SyncSnapshotStatus defines the observed state of SyncSnapshot
            properties:
              conditions:
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              extraSnapshot:
                items:
                  description: CreateSnapshotStatus defines the observed state of
                    CreateSnapshot
                  properties:
                    conditions:
                      items:
                        description: "Condition contains details for one aspect of
                          the current state of this API Resource.\n---\nThis struct
                          is intended for direct use as an array at the field path
                          .status.conditions.  For example,\n\n\n\ttype FooStatus
                          struct{\n\t    // Represents the observations of a foo's
                          current state.\n\t    // Known .status.conditions.type are:
                          \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                          +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    //
                          +listType=map\n\t    // +listMapKey=type\n\t    Conditions
                          []metav1.Condition `json:\"conditions,omitempty\" patchStrategy:\"merge\"
                          patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                          \   // other fields\n\t}"
                        properties:
                          lastTransitionTime:
                            description: |-
                              lastTransitionTime is the last time the condition transitioned from one status to another.
                              This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                            format: date-time
                            type: string
                          message:
                            description: |-
                              message is a human readable message indicating details about the transition.
                              This may be an empty string.
                            maxLength: 32768
                            type: string
                          observedGeneration:
                            description: |-
                              observedGeneration represents the .metadata.generation that the condition was set based upon.
                              For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                              with respect to the current state of the instance.
                            format: int64
                            minimum: 0
                            type: integer
                          reason:
                            description: |-
                              reason contains a programmatic identifier indicating the reason for the condition's last transition.
                              Producers of specific condition types may define expected values and meanings for this field,
                              and whether the values are considered a guaranteed API.
                              The value should be a CamelCase string.
                              This field may not be empty.
                            maxLength: 1024
                            minLength: 1
                            pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                            type: string
                          status:
                            description: status of the condition, one of True, False,
                              Unknown.
                            enum:
                            - "True"
                            - "False"
                            - Unknown
                            type: string
                          type:
                            description: |-
                              type of condition in CamelCase or in foo.example.com/CamelCase.
                              ---
                              Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                              useful (see .node.status.conditions), the ability to deconflict is important.
                              The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                            maxLength: 316
                            pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                            type: string
                        required:
                        - lastTransitionTime
                        - message
                        - reason
                        - status
                        - type
                        type: object
                      type: array
                    creationStatus:
                      type: string
                    creationTime:
                      type: string
                    inUse:
                      type: boolean
                    namespace:
                      type: string
                    pvcName:
                      type: string
                    readyToUse:
                      type: boolean
                    restoreSize:
                      type: string
                    snapshotContentName:
                      type: string
                    snapshotName:
                      type: string
                    snapshotType:
                      type: string
                    volumeSnapshotClassName:
                      type: string
                  type: object
                type: array
              missingSnapshot:
                items:
                  description: CreateSnapshotStatus defines the observed state of
                    CreateSnapshot
                  properties:
                    conditions:
                      items:
                        description: "Condition contains details for one aspect of
                          the current state of this API Resource.\n---\nThis struct
                          is intended for direct use as an array at the field path
                          .status.conditions.  For example,\n\n\n\ttype FooStatus
                          struct{\n\t    // Represents the observations of a foo's
                          current state.\n\t    // Known .status.conditions.type are:
                          \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                          +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    //
                          +listType=map\n\t    // +listMapKey=type\n\t    Conditions
                          []metav1.Condition `json:\"conditions,omitempty\" patchStrategy:\"merge\"
                          patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                          \   // other fields\n\t}"
                        properties:
                          lastTransitionTime:
                            description: |-
                              lastTransitionTime is the last time the condition transitioned from one status to another.
                              This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                            format: date-time
                            type: string
                          message:
                            description: |-
                              message is a human readable message indicating details about the transition.
                              This may be an empty string.
                            maxLength: 32768
                            type: string
                          observedGeneration:
                            description: |-
                              observedGeneration represents the .metadata.generation that the condition was set based upon.
                              For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                              with respect to the current state of the instance.
                            format: int64
                            minimum: 0
                            type: integer
                          reason:
                            description: |-
                              reason contains a programmatic identifier indicating the reason for the condition's last transition.
                              Producers of specific condition types may define expected values and meanings for this field,
                              and whether the values are considered a guaranteed API.
                              The value should be a CamelCase string.
                              This field may not be empty.
                            maxLength: 1024
                            minLength: 1
                            pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                            type: string
                          status:
                            description: status of the condition, one of True, False,
                              Unknown.
                            enum:
                            - "True"
                            - "False"
                            - Unknown
                            type: string
                          type:
                            description: |-
                              type of condition in CamelCase or in foo.example.com/CamelCase.
                              ---
                              Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                              useful (see .node.status.conditions), the ability to deconflict is important.
                              The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                            maxLength: 316
                            pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                            type: string
                        required:
                        - lastTransitionTime
                        - message
                        - reason
                        - status
                        - type
                        type: object
                      type: array
                    creationStatus:
                      type: string
                    creationTime:
                      type: string
                    inUse:
                      type: boolean
                    namespace:
                      type: string
                    pvcName:
                      type: string
                    readyToUse:
                      type: boolean
                    restoreSize:
                      type: string
                    snapshotContentName:
                      type: string
                    snapshotName:
                      type: string
                    snapshotType:
                      type: string
                    volumeSnapshotClassName:
                      type: string
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: createkubesnapshots.snapshot.mfke.io
spec:
  group: snapshot.mfke.io
  names:
    kind: CreateKubeSnapshot
    listKind: CreateKubeSnapshotList
    plural: createkubesnapshots
    singular: createkubesnapshot
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.backupStatus
      name: backupStatus
      type: string
    - jsonPath: .status.message
      name: message
      type: string
    - jsonPath: .status.reason
      name: reason
      type: string
    - jsonPath: .status.runCounter
      name: runCounter
      type: integer
    - jsonPath: .status.ongoingRun.backupStatus
      name: ongoingRun
      type: string
    - jsonPath: .status.ongoingRun.startedAt
      name: ongoingRunStartedAt
      type: string
    - jsonPath: .status.lastSuccessfulRun.backupStatus
      name: lastSuccessfulRun
      type: string
    - jsonPath: .status.lastSuccessfulRun.startedAt
      name: lastSuccessfulRunStartedAt
      type: string
    - jsonPath: .status.lastSuccessfulRun.finishedAt
      name: lastSuccessfulRunFinishedAt
      type: string
    - jsonPath: .status.lastFailedRun.backupStatus
      name: lastFailedRun
      type: string
    - jsonPath: .status.lastFailedRun.startedAt
      name: lastFailedRunStartedAt
      type: string
    - jsonPath: .status.lastFailedRun.finishedAt
      name: lastFailedRunFinishedAt
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: CreateKubeSnapshot is the Schema for the createkubesnapshots
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CreateKubeSnapshotSpec defines the desired state of CreateKubeSnapshot
            properties:
              backupTargets:
                description: BackupTargets is the list of backup targets to use for
                  the snapshot.
                properties:
                  includeClusterResources:
                    description: |-
                      IncludeClusterResources, if true, will include cluster-wide resources in
                      the snapshot. Otherwise, only namespace-scoped resources will be included.
                    type: boolean
                  includeNamespaces:
                    description: |-
                      IncludeNamespaces is the list of namespaces to include in the snapshot.
                      If nil, all namespaces will be included. If empty, no namespaces will be
                      included.
                    items:
                      type: string
                    type: array
                type: object
              runCounter:
                description: |-
                  RunCounter is the counter for the run of the backup. This should be
                  incremented for each run of the backup.


                  Note that the controller will not do anything until this counter is
                  higher than the counter in the status, so while you can set this to any
                  value, it will not have any effect until you properly increment it.
                format: int32
                type: integer
            required:
            - backupTargets
            - runCounter
            type: object
          status:
            description: CreateKubeSnapshotStatus defines the observed state of CreateKubeSnapshot
            properties:
              backupStatus:
                description: BackupStatus is the current status of the snapshot.
                type: string
              lastFailedRunStatus:
                description: |-
                  LastFailedRun is the status of the last failed run of the backup.
                  It is nil if no failed runs have been made.
                properties:
                  backupPodMessage:
                    description: BackupPodMessage is the message from the backup pod,
                      if any.
                    type: string
                  backupPodReason:
                    description: BackupPodReason is the reason for the backup pod
                      status, if any.
                    type: string
                  backupStatus:
                    description: BackupStatus is the current status of the snapshot.
                    type: string
                  finishedAt:
                    description: |-
                      FinishedAt is the time at which the job finished.
                      Internally, this is the time that the kube-dump container has ended.
                      This is nil if the job has not yet finished.
                    format: date-time
                    type: string
                  kubeDumpMessage:
                    description: KubeDumpMessage is the message from the kube-dump
                      container, if any.
                    type: string
                  kubeDumpReason:
                    description: KubeDumpReason is the reason for the kube-dump container
                      status, if any.
                    type: string
                  kubeDumpRestarts:
                    description: |-
                      KubeDumpRestarts is the number of restarts of the kube-dump container.
                      This count should be zero for a successful backup.
                    format: int32
                    type: integer
                  kubeDumpStatus:
                    description: KubeDumpStatus is the status of the kube-dump container.
                    type: string
                  startedAt:
                    description: |-
                      StartedAt is the time at which the job started.
                      Internally, this is the time that the kube-dump pod was created, not the
                      time that the kube-dump container has ended.
                    format: date-time
                    type: string
                required:
                - backupStatus
                - kubeDumpRestarts
                - kubeDumpStatus
                - startedAt
                type: object
              lastSuccessfulRun:
                description: |-
                  LastSuccessfulRun is the status of the last successful run of the backup.
                  It is nil if no successful runs have been made.
                properties:
                  backupPodMessage:
                    description: BackupPodMessage is the message from the backup pod,
                      if any.
                    type: string
                  backupPodReason:
                    description: BackupPodReason is the reason for the backup pod
                      status, if any.
                    type: string
                  backupStatus:
                    description: BackupStatus is the current status of the snapshot.
                    type: string
                  finishedAt:
                    description: |-
                      FinishedAt is the time at which the job finished.
                      Internally, this is the time that the kube-dump container has ended.
                      This is nil if the job has not yet finished.
                    format: date-time
                    type: string
                  kubeDumpMessage:
                    description: KubeDumpMessage is the message from the kube-dump
                      container, if any.
                    type: string
                  kubeDumpReason:
                    description: KubeDumpReason is the reason for the kube-dump container
                      status, if any.
                    type: string
                  kubeDumpRestarts:
                    description: |-
                      KubeDumpRestarts is the number of restarts of the kube-dump container.
                      This count should be zero for a successful backup.
                    format: int32
                    type: integer
                  kubeDumpStatus:
                    description: KubeDumpStatus is the status of the kube-dump container.
                    type: string
                  startedAt:
                    description: |-
                      StartedAt is the time at which the job started.
                      Internally, this is the time that the kube-dump pod was created, not the
                      time that the kube-dump container has ended.
                    format: date-time
                    type: string
                required:
                - backupStatus
                - kubeDumpRestarts
                - kubeDumpStatus
                - startedAt
                type: object
              message:
                description: Message is a message from the controller about the status
                  of the backup.
                type: string
              ongoingRun:
                description: |-
                  OngoingRun is the status of the current run of the backup.
                  It is nil if no runs are currently in progress.
                properties:
                  backupPodMessage:
                    description: BackupPodMessage is the message from the backup pod,
                      if any.
                    type: string
                  backupPodReason:
                    description: BackupPodReason is the reason for the backup pod
                      status, if any.
                    type: string
                  backupStatus:
                    description: BackupStatus is the current status of the snapshot.
                    type: string
                  finishedAt:
                    description: |-
                      FinishedAt is the time at which the job finished.
                      Internally, this is the time that the kube-dump container has ended.
                      This is nil if the job has not yet finished.
                    format: date-time
                    type: string
                  kubeDumpMessage:
                    description: KubeDumpMessage is the message from the kube-dump
                      container, if any.
                    type: string
                  kubeDumpReason:
                    description: KubeDumpReason is the reason for the kube-dump container
                      status, if any.
                    type: string
                  kubeDumpRestarts:
                    description: |-
                      KubeDumpRestarts is the number of restarts of the kube-dump container.
                      This count should be zero for a successful backup.
                    format: int32
                    type: integer
                  kubeDumpStatus:
                    description: KubeDumpStatus is the status of the kube-dump container.
                    type: string
                  startedAt:
                    description: |-
                      StartedAt is the time at which the job started.
                      Internally, this is the time that the kube-dump pod was created, not the
                      time that the kube-dump container has ended.
                    format: date-time
                    type: string
                required:
                - backupStatus
                - kubeDumpRestarts
                - kubeDumpStatus
                - startedAt
                type: object
              reason:
                description: Reason is the reason for the backup status, if any.
                type: string
              runCounter:
                description: |-
                  RunCounter is the counter for the run of the backup. This should be
                  incremented for each run of the backup.
                format: int32
                type: integer
            required:
            - backupStatus
            - runCounter
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}