# Test

This directory contains the end-to-end (`e2e`) integration tests and testing utilities to validate the operator's behavior in a real cluster environment. 

It helps ensure that the backup and restore flows work as expected on OpenStack-based clusters with Cinder CSI dependencies.
