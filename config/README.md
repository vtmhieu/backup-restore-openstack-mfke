# Config

This directory describes the configuration resources required to deploy the controller manager, its RBAC permissions, Custom Resource Definitions (CRDs), and samples.

It is typically managed by Kustomize to package the different manifests together (e.g., CRDs, RBAC, Webhooks, Manager deployment).
