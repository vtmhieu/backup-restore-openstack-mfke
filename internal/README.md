# Internal

This directory contains the private application code, specifically the `controller` logic (Reconcilers) that implements the business logic for the CRDs.

The controllers are:
- `PvcReconciler`
- `PvSnapshotReconciler`
- `RestorePvcReconciler`
- `SchedulerSnapshotReconciler`
