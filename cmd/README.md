# CMD

This directory contains the main entrypoint (`main.go`) for the controller manager. 

The `main.go` file is responsible for initializing the reconcilers, setting up the manager with the cluster configuration, and starting the operator.
